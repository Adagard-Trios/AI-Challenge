# Laptop-sized. Requests, because that is what the scheduler packs against and
# what the HPA computes utilisation from.
- op: replace
  path: /spec/template/spec/containers/0/resources/requests/cpu
  value: "100m"
- op: replace
  path: /spec/template/spec/containers/0/resources/requests/memory
  value: "250Mi"
