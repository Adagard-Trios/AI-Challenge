# Serving-only dependencies for service.py.
#
# Drops the training stack from requirements.txt (optuna, umap-learn, hdbscan,
# mlflow, dagshub, astro-run-dag) — inference only needs the embedding models plus
# scikit-learn to load the IsolationForest.
#
# CPU-only torch: the default Linux wheel drags in ~2.5 GB of nvidia-* packages
# that are useless on a CPU host.
--extra-index-url https://download.pytorch.org/whl/cpu

fastapi
uvicorn[standard]

torch==2.9.1+cpu
transformers>=4.30
sentence-transformers>=2.0

scikit-learn>=1.0
joblib>=1.3
numpy>=1.24
pandas>=2.0

# Language detection (vectorizer picks the per-language embedding model)
fasttext-wheel>=0.9.2
lingua-language-detector>=2.0

pyyaml>=6.0
python-dotenv
